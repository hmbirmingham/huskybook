{
  "name": "huskybook",
  "private": true,
  "version": "0.1.0",
  "description": "Peer-to-peer directory connecting UConn students who offer personal-care services (hair, nails, makeup, braids) with students looking to book one.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,yellow \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "seed": "npm run seed -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
